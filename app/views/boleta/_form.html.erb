<%= form_for(@boletum) do |f| %>
  <% if @boletum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@boletum.errors.count, "error") %> prohibited this boletum from being saved:</h2>

      <ul>
      <% @boletum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :pizza1_id %><br>
    <%= f.text_field :pizza1_id %>
  </div>
  <div class="field">
    <%= f.label :ingrediente_id %><br>
    <%= f.text_field :ingrediente_id %>
  </div>
  <div class="field">
    <%= f.label :usuario_id %><br>
    <%= f.text_field :usuario_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
