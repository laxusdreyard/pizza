<p id="notice"><%= notice %></p>

<h1>Su pedido</h1>

<table>
  <thead>
    <tr>
      <th>Puede</th>
      <th>Generarlo</th>
      <th>A Un Click</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @pizza1s.each do |pizza1| %>
      <tr>
        <td><%= pizza1.nombre %></td>
        <td><%= pizza1.tamano %></td>
        <td><%= pizza1.precio %></td>
        <td><%= link_to 'Show', pizza1 %></td>
        <td><%= link_to 'Edit', edit_pizza1_path(pizza1) %></td>
        <td><%= link_to 'Destroy', pizza1, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Generar Pedido', new_pizza1_path %>
